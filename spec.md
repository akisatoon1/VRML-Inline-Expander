# **VRML Inline展開ツール 仕様書**

## **1\. 概要**

VRMLファイル内に記述された Inline ノードを検出し、参照先ファイルの内容を Group ノードとして展開・埋め込みを行うCLIツール。

## **2\. 実行環境・形式**

* **形式:** CLI（コマンドラインインターフェース）ツール  
* **実行コマンド:**  
  ./program inputfile outputfile

  * inputfile: 処理対象の元VRMLファイルパス  
  * outputfile: 展開後の保存先ファイルパス

## **3\. 前提条件・制約事項**

### **3.1. ファイルフォーマット・エンコーディング**

* **フォーマット:** 入力および参照先ファイルは、有効なVRML 2.0形式である。  
* **文字コード:** すべてのファイルは **UTF-8** エンコーディングである（BOMなし推奨）。Shift\_JIS等は対象外とする。

### **3.2. Inlineノードの記述制約**

処理対象となる Inline ノードは以下の形式に限定する。

* フィールドは url のみを持つ（bboxSize, bboxCenter 等は持たない）。  
* url フィールドの値は、単一の文字列である（例: url "file.wrl"）。リスト形式（url \["file.wrl"\]）は対象外とする。  
* url で指定されるパスは **ローカルファイルパス** のみとする（http:// 等のネットワーク参照は非対応）。

### **3.3. 参照関係の制約**

* **再帰なし:** 参照先のVRMLファイル内には、Inline ノードは存在しない。  
* **重複参照なし:** 同一のファイルを入力ファイル内で複数回 Inline することはない（展開後のDEF名重複回避のため）。  
* **名前空間:** 入力ファイルと参照先ファイルの間で、DEF名の衝突はないものとする。

### **3.4. ROUTE/イベントの制約**

* Inline ノード自体をターゲットとする ROUTE（例: set\_url へのイベント送信）は存在しないものとする。

## **4\. 処理・変換仕様**

### **4.1. パス解決**

* 参照先ファイルのパスは、\*\*inputfile が存在するディレクトリを基準（カレント）\*\*として解決する。

### **4.2. ノードの置換ロジック**

Inline ノードを Group ノードへ以下のように変換する。

1. **ノード型の変更:**  
   * Inline キーワードを Group に変更する。  
2. **childrenフィールドの生成:**  
   * Group ノード内に children フィールドを作成する。  
   * 構文: children \[ ... \]  
3. **コンテンツの埋め込み:**  
   * 参照先ファイルを読み込み、その内容を上記 children の \[\] 内に展開する。  
   * **ヘッダー削除:** 参照先ファイルの先頭にあるヘッダー行（\#VRML V2.0 utf8 等）は削除（または無視）して展開する。

### **4.3. 変換イメージ**

**変換前 (Input):**

```
#VRML V2.0 utf8  
DEF MyModel Inline {  
  url "part.wrl"  
}
```

**参照先 (part.wrl):**

```
#VRML V2.0 utf8  
Shape { geometry Box {} }
```

**変換後 (Output):**

```
#VRML V2.0 utf8  
DEF MyModel Group {  
  children [  
    Shape { geometry Box {} }  
  ]  
}  
```
